<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The shared ISBN rule. Everything downstream of Models picks it up from here. -->
    <ProjectReference Include="..\BookDB.Isbn\BookDB.Isbn.csproj" />
  </ItemGroup>

</Project>
